
const age = 90;

// Writing if else using ternary operator.

age>=18 ? console.log('yes'): console.log("no");

// basic syntax for ternary operator would be conditon ? //true part : //false part

// basic switch case statements used to handle cases:


const options = 1

switch(options){
    case 1 : console.log('volleyball')
    break
    case 2 : console.log('running')
    break
    case 3 : console.log('swimming')
    break
    default: console.log('no sports')
}

/// switch case is makes the code scalable when we are dealing with more number of conditions so its better to use
// switch case then creating if else ladders.